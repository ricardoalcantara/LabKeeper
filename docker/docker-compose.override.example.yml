services:
  min-idp:
    environment:
      # Override for non-localhost access, e.g. http://192.168.1.107:8081
      MIN_IDP_EXTERNAL_URL: http://localhost:8081
      # Generate with: openssl rand -base64 32
      MIN_IDP_MASTER_KEY: "paste-output-of-openssl-rand-base64-32-here"
